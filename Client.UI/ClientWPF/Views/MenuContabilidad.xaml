<Window x:Class="ClientWPF.Views.MenuContabilidad"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Módulo de Contabilidad" Height="600" Width="900"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        MouseDown="Window_MouseDown">

    <Window.Resources>
        <SolidColorBrush x:Key="SurfaceBrush" Color="#F2F0E9"/>
        <SolidColorBrush x:Key="PrimaryTextBrush" Color="#023E38"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#D92027"/>

        <LinearGradientBrush x:Key="BackgroundGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#023E38" Offset="0.0"/>
            <GradientStop Color="#012925" Offset="1.0"/>
        </LinearGradientBrush>

        <Style x:Key="ModuleCardStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
            <Setter Property="Margin" Value="15"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="160"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border CornerRadius="20" Background="Black" Opacity="0.2" Margin="0,5,0,-5">
                                <Border.Effect>
                                    <BlurEffect Radius="15"/>
                                </Border.Effect>
                            </Border>

                            <Border x:Name="border" CornerRadius="20" Background="{TemplateBinding Background}" BorderThickness="0">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#D92027"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#D92027" BlurRadius="20" ShadowDepth="0" Opacity="0.6"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.96" ScaleY="0.96" CenterX="75" CenterY="75"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="LogoutButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="#FF8A8A"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="Transparent" Padding="10,5">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="#FF4D4D"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource BackgroundGradient}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- HEADER -->
            <StackPanel Grid.Row="0" Margin="40,30,40,20">
                <TextBlock Text="MÓDULO DE CONTABILIDAD" FontSize="28" FontWeight="Black" Foreground="{StaticResource SurfaceBrush}" Opacity="0.9" HorizontalAlignment="Center"/>
                <TextBlock Text="Gestión del Plan de Cuentas, Comprobantes y Reportes" FontSize="16" Foreground="#60FFFFFF" HorizontalAlignment="Center" Margin="0,8,0,0"/>
                <Separator Background="#30FFFFFF" Margin="0,20,0,0"/>
            </StackPanel>

            <!-- OPCIONES -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Margin="30,0">
                <UniformGrid Columns="3" Rows="2">

                    <!-- TIPOS DE CUENTA -->
                    <Button Style="{StaticResource ModuleCardStyle}" Click="BtnTipoCuenta_Click">
                        <StackPanel>
                            <TextBlock Text="📋" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                            <TextBlock Text="Tipos de Cuenta" FontWeight="Bold" FontSize="18" TextAlignment="Center"/>
                            <TextBlock Text="Simple 1" FontSize="11" Opacity="0.6" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Button>

                    <!-- PLAN DE CUENTAS -->
                    <Button Style="{StaticResource ModuleCardStyle}" Click="BtnCuenta_Click">
                        <StackPanel>
                            <TextBlock Text="💰" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                            <TextBlock Text="Plan de Cuentas" FontWeight="Bold" FontSize="18" TextAlignment="Center"/>
                            <TextBlock Text="Simple 2" FontSize="11" Opacity="0.6" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Button>

                    <!-- COMPROBANTES -->
                    <Button Style="{StaticResource ModuleCardStyle}" Click="BtnComprobante_Click">
                        <StackPanel>
                            <TextBlock Text="📝" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                            <TextBlock Text="Comprobantes" FontWeight="Bold" FontSize="18" TextAlignment="Center"/>
                            <TextBlock Text="Compleja" FontSize="11" Opacity="0.6" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Button>

                    <!-- BALANCE GENERAL -->
                    <Button Style="{StaticResource ModuleCardStyle}" Click="BtnBalanceGeneral_Click">
                        <StackPanel>
                            <TextBlock Text="📊" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                            <TextBlock Text="Balance General" FontWeight="Bold" FontSize="18" TextAlignment="Center"/>
                            <TextBlock Text="Reporte 1" FontSize="11" Opacity="0.6" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Button>

                    <!-- ESTADO DE RESULTADOS -->
                    <Button Style="{StaticResource ModuleCardStyle}" Click="BtnEstadoResultados_Click">
                        <StackPanel>
                            <TextBlock Text="💹" FontSize="48" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                            <TextBlock Text="Estado de Resultados" FontWeight="Bold" FontSize="18" TextAlignment="Center"/>
                            <TextBlock Text="Reporte 2" FontSize="11" Opacity="0.6" HorizontalAlignment="Center" Margin="0,5,0,0"/>
                        </StackPanel>
                    </Button>

                </UniformGrid>
            </ScrollViewer>

            <!-- FOOTER -->
            <Grid Grid.Row="2" Margin="30,10,30,20">
                <Button Content="← Volver al Menú Principal" 
                        HorizontalAlignment="Center"
                        Style="{StaticResource LogoutButtonStyle}"
                        Click="BtnVolver_Click"/>
            </Grid>
        </Grid>
    </Border>
</Window>