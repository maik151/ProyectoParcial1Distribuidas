<Window x:Class="ClientWPF.Views.ModalExito"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Exito" Height="300" Width="400"
        WindowStyle="None" 
        AllowsTransparency="True" 
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        MouseDown="Window_MouseDown">

    <Window.Resources>
        <Color x:Key="BaseColor">#EBECF0</Color>
        <SolidColorBrush x:Key="BaseBrush" Color="{StaticResource BaseColor}"/>
        <SolidColorBrush x:Key="TextBrush" Color="#5A667F"/>

        <LinearGradientBrush x:Key="AccentGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#7F7FD5" Offset="0.0"/>
            <GradientStop Color="#86A8E7" Offset="0.5"/>
            <GradientStop Color="#91EAE4" Offset="1.0"/>
        </LinearGradientBrush>

        <Style x:Key="NeuButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource BaseBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="shadowLayer" CornerRadius="10" Background="{StaticResource BaseBrush}">
                                <Border.Effect>
                                    <DropShadowEffect Color="#A6ABBD" Direction="315" ShadowDepth="4" BlurRadius="8" Opacity="0.5"/>
                                </Border.Effect>
                            </Border>

                            <Border x:Name="border" CornerRadius="10" Background="{TemplateBinding Background}" BorderThickness="0">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource AccentGradient}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.98" ScaleY="0.98" CenterX="50" CenterY="15"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource BaseBrush}" CornerRadius="20" Margin="10">
        <Border.Effect>
            <DropShadowEffect Color="#909090" BlurRadius="25" ShadowDepth="10" Opacity="0.4"/>
        </Border.Effect>

        <Grid Margin="20">
            <StackPanel VerticalAlignment="Center">

                <TextBlock Text="✔" FontSize="50" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10">
                    <TextBlock.Foreground>
                        <StaticResource ResourceKey="AccentGradient"/>
                    </TextBlock.Foreground>
                    <TextBlock.Effect>
                        <DropShadowEffect Color="#FFFFFF" Direction="135" ShadowDepth="2" BlurRadius="2" Opacity="0.8"/>
                    </TextBlock.Effect>
                </TextBlock>

                <TextBlock Text="LOGIN EXITOSO" FontSize="16" FontWeight="Bold" 
                           Foreground="{StaticResource TextBrush}" HorizontalAlignment="Center" Margin="0,0,0,10"/>

                <TextBlock x:Name="txtMensaje" 
                           Text="Bienvenido al sistema" 
                           FontSize="13" Foreground="#7A869A" 
                           HorizontalAlignment="Center" TextAlignment="Center" 
                           TextWrapping="Wrap" Margin="10,0,10,25"/>

                <Button Content="CONTINUAR" Click="BtnOk_Click" 
                        Width="120" Height="35"
                        Style="{StaticResource NeuButtonStyle}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>