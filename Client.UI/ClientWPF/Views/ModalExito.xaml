<Window x:Class="ClientWPF.Views.ModalExito"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Exito" Height="400" Width="450"
        WindowStyle="None" 
        AllowsTransparency="True" 
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        MouseDown="Window_MouseDown">

    <Window.Resources>
        <!-- PALETA DE COLORES (Consistente con Login/Dashboard) -->
        <SolidColorBrush x:Key="SurfaceBrush" Color="#F2F0E9"/>
        <SolidColorBrush x:Key="PrimaryTextBrush" Color="#023E38"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#D92027"/>
        <SolidColorBrush x:Key="WhiteBrush" Color="#FFFFFF"/>

        <!-- ESTILO DE BOTÓN PRINCIPAL -->
        <Style x:Key="OrganicPrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="45"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="border" CornerRadius="22" Background="{TemplateBinding Background}" BorderThickness="0">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#B91B21"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#D92027" BlurRadius="15" ShadowDepth="0" Opacity="0.6"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.98" ScaleY="0.98" CenterX="60" CenterY="22"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- CONTENEDOR PRINCIPAL (Estilo Card) -->
    <Border Background="{StaticResource SurfaceBrush}" CornerRadius="24" Margin="20">
        <Border.Effect>
            <!-- Sombra fuerte para que flote sobre la aplicación -->
            <DropShadowEffect Color="#000000" BlurRadius="30" ShadowDepth="10" Opacity="0.3" Direction="270"/>
        </Border.Effect>

        <Grid Margin="30">
            <StackPanel VerticalAlignment="Center">

                <!-- ICONO DE ÉXITO -->
                <Border Background="#15023E38" Width="90" Height="90" CornerRadius="45" Margin="0,0,0,25">
                    <TextBlock Text="✔" FontSize="45" FontWeight="Bold" 
                               Foreground="{StaticResource PrimaryTextBrush}" 
                               HorizontalAlignment="Center" VerticalAlignment="Center"
                               Margin="0,4,0,0"/>
                </Border>

                <!-- TÍTULO -->
                <TextBlock Text="¡OPERACIÓN EXITOSA!" FontSize="20" FontWeight="Black" 
                           Foreground="{StaticResource PrimaryTextBrush}" 
                           HorizontalAlignment="Center" Margin="0,0,0,15"/>

                <!-- MENSAJE DINÁMICO (CORREGIDO: Color más oscuro y legible) -->
                <!-- Usamos PrimaryTextBrush con Opacity 0.8 en lugar de color transparente claro -->
                <TextBlock x:Name="txtMensaje" 
                           Text="Bienvenido al sistema" 
                           FontSize="16" 
                           Foreground="{StaticResource PrimaryTextBrush}" Opacity="0.85"
                           FontWeight="SemiBold"
                           HorizontalAlignment="Center" TextAlignment="Center" 
                           TextWrapping="Wrap" Margin="10,0,10,35"/>

                <!-- BOTÓN CONTINUAR -->
                <Button Content="CONTINUAR" Click="BtnOk_Click" 
                        Width="160"
                        Style="{StaticResource OrganicPrimaryButton}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>