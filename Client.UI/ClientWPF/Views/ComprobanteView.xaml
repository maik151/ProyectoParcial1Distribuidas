<Window x:Class="ClientWPF.Views.ComprobanteView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Comprobantes Contables" Height="700" Width="1200"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        MouseDown="Window_MouseDown">

    <Window.Resources>
        <SolidColorBrush x:Key="SurfaceBrush" Color="#F2F0E9"/>
        <SolidColorBrush x:Key="PrimaryTextBrush" Color="#023E38"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#D92027"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="#51CF66"/>
        <SolidColorBrush x:Key="LightHoverBrush" Color="#10023E38"/>

        <LinearGradientBrush x:Key="BackgroundGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#023E38" Offset="0.0"/>
            <GradientStop Color="#012925" Offset="1.0"/>
        </LinearGradientBrush>

        <Style x:Key="SquareInputStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="BorderBrush" Value="#30023E38"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="SquarePrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="4" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#B91B21"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SquareSecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryTextBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="4" Background="{TemplateBinding Background}" BorderThickness="1" BorderBrush="{StaticResource PrimaryTextBrush}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource LightHoverBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="GridActionButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bdr" Background="Transparent" CornerRadius="4" Padding="5">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bdr" Property="Background" Value="#20000000"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RowHeaderWidth" Value="0"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#20023E38"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="ColumnHeaderStyle">
                <Setter.Value>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Padding" Value="10"/>
                        <Setter Property="BorderThickness" Value="0,0,0,2"/>
                        <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                    </Style>
                </Setter.Value>
            </Setter>
            <Setter Property="CellStyle">
                <Setter.Value>
                    <Style TargetType="DataGridCell">
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Background="Transparent" Padding="10,8">
                                        <ContentPresenter VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Setter.Value>
            </Setter>
            <Setter Property="RowStyle">
                <Setter.Value>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="Transparent"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource LightHoverBrush}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource BackgroundGradient}">
        <Border Background="{StaticResource SurfaceBrush}" CornerRadius="12" Margin="20">
            <Border.Effect>
                <DropShadowEffect Color="#000000" BlurRadius="20" ShadowDepth="5" Opacity="0.3"/>
            </Border.Effect>

            <Grid Margin="25">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- HEADER -->
                <Grid Grid.Row="0" Margin="0,0,0,20">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <Path Data="M14,2H6A2,2 0 0,0 4,4V20A2,2 0 0,0 6,22H18A2,2 0 0,0 20,20V8L14,2M18,20H6V4H13V9H18V20Z" 
                              Fill="{StaticResource AccentBrush}" Height="24" Width="24" Stretch="Uniform" Margin="0,0,10,0"/>
                        <TextBlock Text="COMPROBANTES CONTABLES" FontSize="22" FontWeight="Black" Foreground="{StaticResource PrimaryTextBrush}"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Style="{StaticResource GridActionButton}" Click="BtnRecargar_Click" ToolTip="Recargar" Margin="0,0,10,0" Width="35" Height="35">
                            <Path Data="M12,4V1L8,5L12,9V6A6,6 0 1,1 6,12H4A8,8 0 1,0 12,4Z" 
                                  Fill="{StaticResource PrimaryTextBrush}" Stretch="Uniform" Width="16" Height="16"/>
                        </Button>

                        <Button Style="{StaticResource GridActionButton}" Click="BtnCerrar_Click" ToolTip="Cerrar" Width="35" Height="35">
                            <Path Data="M19,6.41L17.59,5L12,10.59L6.41,5L5,6.41L10.59,12L5,17.59L6.41,19L12,13.41L17.59,19L19,17.59L13.41,12L19,6.41Z" 
                                  Fill="{StaticResource AccentBrush}" Stretch="Uniform" Width="14" Height="14"/>
                        </Button>
                    </StackPanel>
                </Grid>

                <!-- CONTENIDO -->
                <Grid Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="400"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- PANEL IZQUIERDO: LISTA -->
                    <Border Grid.Column="0" Background="#FFFFFF" CornerRadius="8" Margin="0,0,15,0" Padding="15" BorderBrush="#20000000" BorderThickness="1">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <TextBlock Grid.Row="0" Text="Lista de Comprobantes" FontSize="14" FontWeight="Bold" Foreground="{StaticResource PrimaryTextBrush}" Margin="0,0,0,15"/>

                            <DataGrid Grid.Row="1" x:Name="gridComprobantes" SelectionChanged="GridComprobantes_SelectionChanged">
                                <DataGrid.Columns>
                                    <DataGridTextColumn Header="Número" Binding="{Binding Numero}" Width="80"/>
                                    <DataGridTextColumn Header="Fecha" Binding="{Binding Fecha, StringFormat=dd/MM/yyyy}" Width="100"/>
                                    <DataGridTextColumn Header="Observaciones" Binding="{Binding Observaciones}" Width="*"/>
                                </DataGrid.Columns>
                            </DataGrid>
                        </Grid>
                    </Border>

                    <!-- PANEL DERECHO: FORMULARIO -->
                    <Border Grid.Column="1" Background="#FFFFFF" CornerRadius="8" Padding="20" BorderBrush="#20000000" BorderThickness="1">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>

                            <!-- CABECERA -->
                            <StackPanel Grid.Row="0">
                                <TextBlock Text="Datos de Cabecera" FontSize="16" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" Margin="0,0,0,15"/>

                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="15"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="Número" Foreground="{StaticResource PrimaryTextBrush}" FontWeight="SemiBold" Margin="2,0,0,5"/>
                                        <Border Style="{StaticResource SquareInputStyle}">
                                            <TextBox x:Name="txtNumero" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" Padding="10,0" FontSize="14"/>
                                        </Border>
                                    </StackPanel>

                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="Fecha" Foreground="{StaticResource PrimaryTextBrush}" FontWeight="SemiBold" Margin="2,0,0,5"/>
                                        <Border Style="{StaticResource SquareInputStyle}">
                                            <DatePicker x:Name="dpFecha" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" Padding="10,0" FontSize="14"/>
                                        </Border>
                                    </StackPanel>
                                </Grid>

                                <TextBlock Text="Observaciones" Foreground="{StaticResource PrimaryTextBrush}" FontWeight="SemiBold" Margin="2,0,0,5"/>
                                <Border Style="{StaticResource SquareInputStyle}" Height="60">
                                    <TextBox x:Name="txtObservaciones" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Top" Padding="10" FontSize="14" TextWrapping="Wrap" AcceptsReturn="True"/>
                                </Border>
                            </StackPanel>

                            <!-- AGREGAR DETALLE -->
                            <Border Grid.Row="1" Background="#F5F7FA" CornerRadius="8" Padding="15" Margin="0,15,0,15">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="10"/>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="10"/>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="10"/>
                                        <ColumnDefinition Width="50"/>
                                    </Grid.ColumnDefinitions>

                                    <StackPanel Grid.Column="0">
                                        <TextBlock Text="Cuenta" Foreground="{StaticResource PrimaryTextBrush}" FontWeight="SemiBold" FontSize="11" Margin="0,0,0,5"/>
                                        <ComboBox x:Name="cboCuenta" Height="35" DisplayMemberPath="Nombre" SelectedValuePath="CuentaId" FontSize="13"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="2">
                                        <TextBlock Text="Debe" Foreground="{StaticResource PrimaryTextBrush}" FontWeight="SemiBold" FontSize="11" Margin="0,0,0,5"/>
                                        <TextBox x:Name="txtDebe" Height="35" VerticalContentAlignment="Center" Padding="8,0" FontSize="13" Text="0"/>
                                    </StackPanel>

                                    <StackPanel Grid.Column="4">
                                        <TextBlock Text="Haber" Foreground="{StaticResource PrimaryTextBrush}" FontWeight="SemiBold" FontSize="11" Margin="0,0,0,5"/>
                                        <TextBox x:Name="txtHaber" Height="35" VerticalContentAlignment="Center" Padding="8,0" FontSize="13" Text="0"/>
                                    </StackPanel>

                                    <Button Grid.Column="6" Content="+" Click="BtnAgregarDetalle_Click" 
                                            Background="{StaticResource AccentBrush}" Foreground="White" 
                                            BorderThickness="0" Height="35" Width="50" VerticalAlignment="Bottom" 
                                            FontWeight="Bold" FontSize="18" Cursor="Hand" ToolTip="Agregar"/>
                                </Grid>
                            </Border>

                            <!-- GRID DETALLE -->
                            <Border Grid.Row="2" Background="Transparent">
                                <DataGrid x:Name="gridDetalle">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="Código" Binding="{Binding CodigoCuenta}" Width="100"/>
                                        <DataGridTextColumn Header="Cuenta" Binding="{Binding NombreCuenta}" Width="*"/>
                                        <DataGridTextColumn Header="Debe" Binding="{Binding CantidadDebe, StringFormat=N2}" Width="100"/>
                                        <DataGridTextColumn Header="Haber" Binding="{Binding CantidadHaber, StringFormat=N2}" Width="100"/>

                                        <DataGridTemplateColumn Header="Acc" Width="60">
                                            <DataGridTemplateColumn.CellTemplate>
                                                <DataTemplate>
                                                    <Button Click="BtnEliminarDetalle_Click" Background="Transparent" BorderThickness="0" Cursor="Hand" ToolTip="Eliminar">
                                                        <Path Data="M19,4H15.5L14.5,3H9.5L8.5,4H5V6H19M6,19A2,2 0 0,0 8,21H16A2,2 0 0,0 18,19V7H6V19Z" 
                                                              Fill="{StaticResource AccentBrush}" Width="14" Height="14" Stretch="Uniform"/>
                                                    </Button>
                                                </DataTemplate>
                                            </DataGridTemplateColumn.CellTemplate>
                                        </DataGridTemplateColumn>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </Border>

                            <!-- TOTALES Y BOTONES -->
                            <Grid Grid.Row="3" Margin="0,15,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="200"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                                    <TextBlock Text="Total Debe: " FontWeight="Bold" Foreground="{StaticResource PrimaryTextBrush}"/>
                                    <TextBlock x:Name="txtTotalDebe" Text="0.00" FontWeight="Bold" Foreground="{StaticResource PrimaryTextBrush}" Margin="5,0,20,0"/>

                                    <TextBlock Text="Total Haber: " FontWeight="Bold" Foreground="{StaticResource PrimaryTextBrush}"/>
                                    <TextBlock x:Name="txtTotalHaber" Text="0.00" FontWeight="Bold" Foreground="{StaticResource PrimaryTextBrush}" Margin="5,0,20,0"/>

                                    <TextBlock x:Name="txtEstadoCuadre" Text="⚠ Descuadrado" FontWeight="Bold" Foreground="{StaticResource AccentBrush}"/>
                                </StackPanel>

                                <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                                    <Button Content="Nuevo" Click="BtnNuevo_Click" Style="{StaticResource SquareSecondaryButton}" Width="90" Height="40" Margin="0,0,10,0"/>
                                    <Button Content="Guardar" Click="BtnGuardar_Click" Style="{StaticResource SquarePrimaryButton}" Width="90" Height="40"/>
                                </StackPanel>
                            </Grid>
                        </Grid>
                    </Border>
                </Grid>
            </Grid>
        </Border>
    </Border>
</Window>