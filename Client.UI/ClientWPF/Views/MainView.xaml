<Window x:Class="ClientWPF.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Menú Principal" Height="600" Width="950"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        MouseDown="Window_MouseDown">

    <Window.Resources>
        <Color x:Key="BaseColor">#EBECF0</Color>
        <SolidColorBrush x:Key="BaseBrush" Color="{StaticResource BaseColor}"/>
        <SolidColorBrush x:Key="TextBrush" Color="#5A667F"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#4D7CFE"/>

        <Style x:Key="ModuleCardStyle" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource BaseBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Margin" Value="15"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="shadow" CornerRadius="20" Background="{StaticResource BaseBrush}">
                                <Border.Effect>
                                    <DropShadowEffect Color="#A6ABBD" Direction="315" ShadowDepth="6" BlurRadius="12" Opacity="0.5"/>
                                </Border.Effect>
                            </Border>
                            <Border CornerRadius="20" Background="Transparent" Margin="-2,-2,0,0">
                                <Border.Effect>
                                    <DropShadowEffect Color="#FFFFFF" Direction="135" ShadowDepth="3" BlurRadius="5" Opacity="0.8"/>
                                </Border.Effect>
                            </Border>
                            <Border x:Name="border" CornerRadius="20" Background="{TemplateBinding Background}" BorderThickness="0">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#F0F3F7"/>
                                <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.96" ScaleY="0.96" CenterX="75" CenterY="75"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource BaseBrush}" CornerRadius="30" Padding="30">
        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="30" ShadowDepth="0" Opacity="0.2"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="0,0,0,20">
                <TextBlock Text="SISTEMA INTEGRADO" FontSize="28" FontWeight="Bold" Foreground="{StaticResource TextBrush}" HorizontalAlignment="Center"/>
                <TextBlock x:Name="txtBienvenida" Text="Cargando..." FontSize="16" Foreground="#8898AA" HorizontalAlignment="Center" Margin="0,5,0,0"/>
            </StackPanel>

            <UniformGrid Grid.Row="1" Columns="3" Rows="2">

                <Button x:Name="btnSeguridad" Style="{StaticResource ModuleCardStyle}" Click="BtnSeguridad_Click">
                    <StackPanel>
                        <TextBlock Text="🔒" FontSize="40" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="Seguridad" FontWeight="Bold" FontSize="16"/>
                    </StackPanel>
                </Button>

                <Button Style="{StaticResource ModuleCardStyle}" Click="BtnContabilidad_Click">
                    <StackPanel>
                        <TextBlock Text="📊" FontSize="40" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="Contabilidad" FontWeight="Bold" FontSize="16"/>
                    </StackPanel>
                </Button>

                <Button Style="{StaticResource ModuleCardStyle}" Click="BtnActivos_Click">
                    <StackPanel>
                        <TextBlock Text="🏢" FontSize="40" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="Activos" FontWeight="Bold" FontSize="16"/>
                    </StackPanel>
                </Button>

                <Button Style="{StaticResource ModuleCardStyle}" Click="BtnMantenimiento_Click">
                    <StackPanel>
                        <TextBlock Text="🔧" FontSize="40" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="Mantenimiento" FontWeight="Bold" FontSize="16"/>
                    </StackPanel>
                </Button>

                <Button Style="{StaticResource ModuleCardStyle}" Click="BtnBiblioteca_Click">
                    <StackPanel>
                        <TextBlock Text="📚" FontSize="40" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock Text="Biblioteca" FontWeight="Bold" FontSize="16"/>
                    </StackPanel>
                </Button>

            </UniformGrid>

            <Button Grid.Row="2" Content="Cerrar Sesión" 
                    HorizontalAlignment="Right" VerticalAlignment="Bottom"
                    Foreground="#FF6B6B" Background="Transparent" BorderThickness="0" Cursor="Hand" FontWeight="Bold"
                    Click="BtnLogout_Click"/>
        </Grid>
    </Border>
</Window>