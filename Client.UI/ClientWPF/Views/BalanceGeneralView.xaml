<Window x:Class="ClientWPF.Views.BalanceGeneralView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Balance General" Height="700" Width="1000"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        MouseDown="Window_MouseDown">

    <Window.Resources>
        <Color x:Key="BaseColor">#EBECF0</Color>
        <SolidColorBrush x:Key="BaseBrush" Color="{StaticResource BaseColor}"/>
        <SolidColorBrush x:Key="TextBrush" Color="#5A667F"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#4D7CFE"/>
        <SolidColorBrush x:Key="DangerBrush" Color="#FF6B6B"/>

        <Style x:Key="NeuInputStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Background" Value="{StaticResource BaseBrush}"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Margin" Value="0,0,0,10"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect Color="#FFFFFF" Direction="135" ShadowDepth="2" BlurRadius="2" Opacity="1"/>
                </Setter.Value>
            </Setter>
            <Setter Property="BorderBrush" Value="#D1D9E6"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>

        <Style x:Key="NeuIconButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource BaseBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Border x:Name="shadow" CornerRadius="10" Background="{StaticResource BaseBrush}">
                                <Border.Effect>
                                    <DropShadowEffect Color="#A6ABBD" Direction="315" ShadowDepth="3" BlurRadius="6" Opacity="0.5"/>
                                </Border.Effect>
                            </Border>
                            <Border x:Name="border" CornerRadius="10" Background="{TemplateBinding Background}" BorderThickness="0">
                                <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10,5"/>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.95" ScaleY="0.95" CenterX="20" CenterY="15"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RowHeaderWidth" Value="0"/>
            <Setter Property="GridLinesVisibility" Value="None"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="ColumnHeaderStyle">
                <Setter.Value>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="{StaticResource TextBrush}"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Padding" Value="10,10"/>
                    </Style>
                </Setter.Value>
            </Setter>
            <Setter Property="CellStyle">
                <Setter.Value>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="10"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border BorderThickness="0,0,0,1" BorderBrush="#D1D9E6" Background="Transparent">
                                        <ContentPresenter VerticalAlignment="Center" />
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
                                <Setter Property="Background" Value="Transparent"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border Background="{StaticResource BaseBrush}" CornerRadius="20" Padding="20">
        <Border.Effect>
            <DropShadowEffect Color="#000000" BlurRadius="20" ShadowDepth="0" Opacity="0.2"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- HEADER -->
            <Grid Grid.Row="0" Margin="0,0,0,20">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="📊" FontSize="25" Margin="0,0,10,0"/>
                    <TextBlock Text="BALANCE GENERAL" FontSize="22" FontWeight="Bold" Foreground="{StaticResource TextBrush}"/>
                </StackPanel>

                <Button Style="{StaticResource NeuIconButton}" Click="BtnCerrar_Click" ToolTip="Cerrar" Width="40" Height="40" HorizontalAlignment="Right">
                    <Path Data="M19,6.41L17.59,5L12,10.59L6.41,5L5,6.41L10.59,12L5,17.59L6.41,19L12,13.41L17.59,19L19,17.59L13.41,12L19,6.41Z" 
                          Fill="{StaticResource DangerBrush}" Stretch="Uniform" Width="15" Height="15"/>
                </Button>
            </Grid>

            <!-- FILTROS -->
            <Border Grid.Row="1" Background="{StaticResource BaseBrush}" CornerRadius="15" Padding="20" Margin="0,0,0,20">
                <Border.Effect>
                    <DropShadowEffect Color="#A6ABBD" Direction="315" ShadowDepth="5" BlurRadius="10" Opacity="0.3"/>
                </Border.Effect>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="20"/>
                        <ColumnDefinition Width="150"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0">
                        <TextBlock Text="Fecha Inicio" Foreground="{StaticResource TextBrush}" FontWeight="Bold" Margin="5,0,0,5"/>
                        <Border Style="{StaticResource NeuInputStyle}">
                            <DatePicker x:Name="dpFechaInicio" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" Padding="10,0" FontSize="14"/>
                        </Border>
                    </StackPanel>

                    <StackPanel Grid.Column="2">
                        <TextBlock Text="Fecha Fin" Foreground="{StaticResource TextBrush}" FontWeight="Bold" Margin="5,0,0,5"/>
                        <Border Style="{StaticResource NeuInputStyle}">
                            <DatePicker x:Name="dpFechaFin" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" Padding="10,0" FontSize="14"/>
                        </Border>
                    </StackPanel>

                    <Button Grid.Column="4" Content="Generar Reporte" Click="BtnGenerar_Click" 
                            Style="{StaticResource NeuIconButton}" Height="40" VerticalAlignment="Bottom" 
                            Foreground="{StaticResource AccentBrush}" FontWeight="Bold"/>
                </Grid>
            </Border>

            <!-- TABLA -->
            <Border Grid.Row="2" Background="{StaticResource BaseBrush}" CornerRadius="15" Padding="10">
                <DataGrid x:Name="gridBalance">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Código" Binding="{Binding Codigo}" Width="150"/>
                        <DataGridTextColumn Header="Cuenta" Binding="{Binding Nombre}" Width="*"/>
                        <DataGridTextColumn Header="Nivel" Binding="{Binding Nivel}" Width="80"/>
                        <DataGridTextColumn Header="Saldo" Binding="{Binding Saldo, StringFormat=N2}" Width="150">
                            <DataGridTextColumn.ElementStyle>
                                <Style TargetType="TextBlock">
                                    <Setter Property="HorizontalAlignment" Value="Right"/>
                                    <Setter Property="FontWeight" Value="Bold"/>
                                </Style>
                            </DataGridTextColumn.ElementStyle>
                        </DataGridTextColumn>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>
        </Grid>
    </Border>
</Window>