<Window x:Class="ClientWPF.Views.MantenimientoView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Mantenimiento" Height="700" Width="1100"
        WindowStartupLocation="CenterScreen"
        WindowStyle="None" 
        AllowsTransparency="True" 
        Background="Transparent"
        ResizeMode="CanResize"
        MouseDown="Window_MouseDown">

    <Window.Resources>
        <!-- PALETA DE COLORES -->
        <SolidColorBrush x:Key="SurfaceBrush" Color="#F2F0E9"/>
        <SolidColorBrush x:Key="PrimaryTextBrush" Color="#023E38"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#D92027"/>
        <SolidColorBrush x:Key="WhiteBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="LightHoverBrush" Color="#10023E38"/>

        <!-- FONDO DEGRADADO -->
        <LinearGradientBrush x:Key="BackgroundGradient" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#023E38" Offset="0.0"/>
            <GradientStop Color="#012925" Offset="1.0"/>
        </LinearGradientBrush>

        <!-- ESTILOS BÁSICOS (Cuadrados) -->
        <Style x:Key="SquareInputStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="4"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Margin" Value="0,0,0,15"/>
            <Setter Property="BorderBrush" Value="#30023E38"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="2"/>
        </Style>

        <Style x:Key="SquarePrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource SurfaceBrush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="4" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#B91B21"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.98" ScaleY="0.98" CenterX="50" CenterY="20"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SquareSecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryTextBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="4" Background="{TemplateBinding Background}" BorderThickness="1" BorderBrush="{StaticResource PrimaryTextBrush}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource LightHoverBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ESTILO BOTÓN ACCIÓN PEQUEÑO (Para la grid o agregar detalle) -->
        <Style x:Key="SmallActionButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryTextBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bdr" CornerRadius="4" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bdr" Property="Background" Value="{StaticResource AccentBrush}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ESTILO TABCONTROL PERSONALIZADO -->
        <Style TargetType="TabItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Grid>
                            <Border Name="Border" Margin="0,0,20,0" Background="Transparent" 
                                    BorderBrush="Transparent" BorderThickness="0,0,0,3" Padding="10,10">
                                <ContentPresenter x:Name="ContentSite" VerticalAlignment="Center" 
                                                  HorizontalAlignment="Center" ContentSource="Header"/>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
                                <Setter Property="FontWeight" Value="Black"/>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="False">
                                <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
                                <Setter Property="FontWeight" Value="SemiBold"/>
                                <Setter Property="Opacity" Value="0.6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- ESTILO DATAGRID (Copiado de UsuariosView) -->
        <Style TargetType="DataGrid">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="RowHeaderWidth" Value="0"/>
            <Setter Property="GridLinesVisibility" Value="Horizontal"/>
            <Setter Property="HorizontalGridLinesBrush" Value="#20023E38"/>
            <Setter Property="VerticalGridLinesBrush" Value="Transparent"/>
            <Setter Property="AutoGenerateColumns" Value="False"/>
            <Setter Property="CanUserAddRows" Value="False"/>
            <Setter Property="IsReadOnly" Value="True"/>
            <Setter Property="HeadersVisibility" Value="Column"/>
            <Setter Property="ColumnHeaderStyle">
                <Setter.Value>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="Background" Value="Transparent"/>
                        <Setter Property="Foreground" Value="{StaticResource PrimaryTextBrush}"/>
                        <Setter Property="FontWeight" Value="Bold"/>
                        <Setter Property="Padding" Value="10,10"/>
                        <Setter Property="FontSize" Value="13"/>
                        <Setter Property="BorderThickness" Value="0,0,0,2"/>
                        <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                    </Style>
                </Setter.Value>
            </Setter>
            <Setter Property="CellStyle">
                <Setter.Value>
                    <Style TargetType="DataGridCell">
                        <Setter Property="Padding" Value="10"/>
                        <Setter Property="Foreground" Value="#023E38"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridCell">
                                    <Border Background="Transparent">
                                        <ContentPresenter VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
                                <Setter Property="FontWeight" Value="Bold"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Setter.Value>
            </Setter>
            <Setter Property="RowStyle">
                <Setter.Value>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="Transparent"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource LightHoverBrush}"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <!-- ESTRUCTURA PRINCIPAL -->
    <Border Background="{StaticResource BackgroundGradient}">
        <!-- CARD PRINCIPAL (Radius 12) -->
        <Border Background="{StaticResource SurfaceBrush}" CornerRadius="12" Margin="20">
            <Border.Effect>
                <DropShadowEffect Color="#000000" BlurRadius="20" ShadowDepth="5" Opacity="0.3"/>
            </Border.Effect>

            <Grid Margin="25">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <!-- Header -->
                    <RowDefinition Height="*"/>
                    <!-- Contenido Tabs -->
                </Grid.RowDefinitions>

                <!-- HEADER -->
                <Grid Grid.Row="0" Margin="0,0,0,20">
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                        <!-- Icono Mantenimiento -->
                        <Path Data="M13.78 15.3L19.78 21.3L21.89 19.14L15.89 13.14L13.78 15.3M17.5 10.1C17.11 10.1 16.69 10.05 16.36 9.91L4.9 21.35L2.05 18.5L13.5 7.05C13.36 6.72 13.31 6.3 13.31 5.91C13.31 4.17 14.73 2.75 16.47 2.75C18.21 2.75 19.63 4.17 19.63 5.91S18.21 9.07 16.47 9.07C16.3 9.07 16.12 9.06 15.95 9.03L15.65 9.33L17.5 11.18C17.5 10.81 17.5 10.45 17.5 10.1Z" 
                              Fill="{StaticResource AccentBrush}" Height="24" Width="24" Stretch="Uniform" Margin="0,0,10,0"/>
                        <TextBlock Text="MÓDULO DE MANTENIMIENTO" FontSize="22" FontWeight="Black" Foreground="{StaticResource PrimaryTextBrush}" VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- Botón Cerrar (Estilo consistente con otras vistas) -->
                    <Button Click="BtnCerrar_Click" HorizontalAlignment="Right" VerticalAlignment="Center" 
                            Background="Transparent" BorderThickness="0" Cursor="Hand" ToolTip="Cerrar">
                        <Path Data="M19,6.41L17.59,5L12,10.59L6.41,5L5,6.41L10.59,12L5,17.59L6.41,19L12,13.41L17.59,19L19,17.59L13.41,12L19,6.41Z" 
                              Fill="{StaticResource AccentBrush}" Width="16" Height="16" Stretch="Uniform"/>
                    </Button>
                </Grid>

                <!-- TABS -->
                <TabControl Grid.Row="1" Background="Transparent" BorderThickness="0">

                    <!-- PESTAÑA 1: REGISTRO -->
                    <TabItem Header="REGISTRO">
                        <Grid Margin="0,20,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="320"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- COLUMNA IZQUIERDA: DATOS GENERALES -->
                            <Border Grid.Column="0" Background="#FFFFFF" CornerRadius="8" Margin="0,0,20,0" Padding="20" BorderBrush="#20000000" BorderThickness="1">
                                <StackPanel>
                                    <TextBlock Text="Datos Generales" FontSize="16" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" Margin="0,0,0,15"/>

                                    <TextBlock Text="Fecha" Foreground="{StaticResource PrimaryTextBrush}" FontWeight="SemiBold" Margin="2,0,0,5"/>
                                    <Border Style="{StaticResource SquareInputStyle}">
                                        <DatePicker x:Name="dpFecha" BorderThickness="0" VerticalContentAlignment="Center" Background="Transparent"/>
                                    </Border>

                                    <TextBlock Text="Responsable" Foreground="{StaticResource PrimaryTextBrush}" FontWeight="SemiBold" Margin="2,0,0,5"/>
                                    <Border Style="{StaticResource SquareInputStyle}">
                                        <TextBox x:Name="txtResponsable" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" Padding="5,0" FontSize="14" Foreground="{StaticResource PrimaryTextBrush}"/>
                                    </Border>

                                    <Separator Background="#20000000" Margin="0,20,0,20"/>

                                    <TextBlock Text="Total Acumulado:" FontWeight="Bold" Foreground="{StaticResource PrimaryTextBrush}"/>
                                    <TextBlock x:Name="lblTotal" Text="$ 0.00" FontSize="32" FontWeight="Black" Foreground="{StaticResource PrimaryTextBrush}" Margin="0,5,0,0"/>

                                    <Button Content="GUARDAR TODO" Click="BtnGuardar_Click" Margin="0,30,0,0" Style="{StaticResource SquarePrimaryButton}"/>
                                </StackPanel>
                            </Border>

                            <!-- COLUMNA DERECHA: DETALLES -->
                            <Grid Grid.Column="1">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="*"/>
                                </Grid.RowDefinitions>

                                <!-- BARRA DE AGREGAR DETALLE -->
                                <Border Grid.Row="0" Background="#FFFFFF" CornerRadius="8" Padding="15" Margin="0,0,0,15" BorderBrush="#20000000" BorderThickness="1">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="2*"/>
                                            <ColumnDefinition Width="2*"/>
                                            <ColumnDefinition Width="1*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <StackPanel Grid.Column="0" Margin="0,0,10,0">
                                            <TextBlock Text="Activo" FontWeight="SemiBold" FontSize="12" Foreground="{StaticResource PrimaryTextBrush}" Margin="0,0,0,2"/>
                                            <Border Style="{StaticResource SquareInputStyle}" Margin="0">
                                                <ComboBox x:Name="cboActivo" BorderThickness="0" Background="Transparent" VerticalContentAlignment="Center"/>
                                            </Border>
                                        </StackPanel>

                                        <StackPanel Grid.Column="1" Margin="0,0,10,0">
                                            <TextBlock Text="Actividad" FontWeight="SemiBold" FontSize="12" Foreground="{StaticResource PrimaryTextBrush}" Margin="0,0,0,2"/>
                                            <Border Style="{StaticResource SquareInputStyle}" Margin="0">
                                                <ComboBox x:Name="cboActividad" BorderThickness="0" Background="Transparent" VerticalContentAlignment="Center"/>
                                            </Border>
                                        </StackPanel>

                                        <StackPanel Grid.Column="2" Margin="0,0,10,0">
                                            <TextBlock Text="Costo ($)" FontWeight="SemiBold" FontSize="12" Foreground="{StaticResource PrimaryTextBrush}" Margin="0,0,0,2"/>
                                            <Border Style="{StaticResource SquareInputStyle}" Margin="0">
                                                <TextBox x:Name="txtCosto" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" Padding="5,0"/>
                                            </Border>
                                        </StackPanel>

                                        <Button Grid.Column="3" Content="+" Click="BtnAgregar_Click" Width="40" Height="40" VerticalAlignment="Bottom" Style="{StaticResource SmallActionButton}" ToolTip="Agregar Detalle"/>
                                    </Grid>
                                </Border>

                                <!-- GRID DETALLES -->
                                <Border Grid.Row="1" Background="Transparent" CornerRadius="8">
                                    <DataGrid x:Name="gridDetalle" AutoGenerateColumns="False">
                                        <DataGrid.Columns>
                                            <DataGridTextColumn Header="ACTIVO" Binding="{Binding NombreActivo}" Width="*"/>
                                            <DataGridTextColumn Header="ACTIVIDAD" Binding="{Binding NombreActividad}" Width="*"/>
                                            <DataGridTextColumn Header="COSTO" Binding="{Binding Valor, StringFormat=C2}" Width="100" FontWeight="Bold"/>

                                            <DataGridTemplateColumn Width="60">
                                                <DataGridTemplateColumn.CellTemplate>
                                                    <DataTemplate>
                                                        <Button Click="BtnQuitarDetalle_Click" Width="24" Height="24" Background="Transparent" BorderThickness="0" Cursor="Hand" ToolTip="Quitar">
                                                            <Path Data="M19,4H15.5L14.5,3H9.5L8.5,4H5V6H19M6,19A2,2 0 0,0 8,21H16A2,2 0 0,0 18,19V7H6V19Z" 
                                                                  Fill="{StaticResource AccentBrush}" Stretch="Uniform" Margin="4"/>
                                                        </Button>
                                                    </DataTemplate>
                                                </DataGridTemplateColumn.CellTemplate>
                                            </DataGridTemplateColumn>
                                        </DataGrid.Columns>
                                    </DataGrid>
                                </Border>
                            </Grid>
                        </Grid>
                    </TabItem>

                    <!-- PESTAÑA 2: CATÁLOGOS -->
                    <TabItem Header="CATÁLOGOS">
                        <Grid Margin="0,20,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

                            <!-- PANEL ACTIVIDADES -->
                            <Border Grid.Column="0" Background="#FFFFFF" CornerRadius="8" Padding="15" Margin="0,0,10,0" BorderBrush="#20000000" BorderThickness="1">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Text="Actividades de Mantenimiento" FontSize="16" FontWeight="Bold" Foreground="{StaticResource AccentBrush}" Margin="0,0,0,15"/>

                                    <Grid Grid.Row="1" Margin="0,0,0,15">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="80"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <Border Grid.Column="0" Style="{StaticResource SquareInputStyle}" Margin="0,0,5,0" Height="35">
                                            <TextBox x:Name="txtCodAct" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" Padding="5,0" ToolTip="Código"/>
                                        </Border>

                                        <Border Grid.Column="1" Style="{StaticResource SquareInputStyle}" Margin="0,0,5,0" Height="35">
                                            <TextBox x:Name="txtNomAct" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" Padding="5,0" ToolTip="Nombre Actividad"/>
                                        </Border>

                                        <Button Grid.Column="2" Content="Guardar" Click="BtnGuardarActividad_Click" Width="80" Height="35" Style="{StaticResource SquareSecondaryButton}"/>
                                    </Grid>

                                    <DataGrid x:Name="gridActividades" Grid.Row="2" AutoGenerateColumns="True"/>
                                </Grid>
                            </Border>

                            <!-- PANEL ACTIVOS -->
                            <Border Grid.Column="1" Background="#FFFFFF" CornerRadius="8" Padding="15" Margin="10,0,0,0" BorderBrush="#20000000" BorderThickness="1">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>

                                    <TextBlock Text="Activos (Referencia Local)" FontSize="16" FontWeight="Bold" Foreground="{StaticResource PrimaryTextBrush}" Margin="0,0,0,15"/>

                                    <Grid Grid.Row="1" Margin="0,0,0,15">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="80"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <Border Grid.Column="0" Style="{StaticResource SquareInputStyle}" Margin="0,0,5,0" Height="35">
                                            <TextBox x:Name="txtIdActivo" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" Padding="5,0" ToolTip="ID Activo"/>
                                        </Border>

                                        <Border Grid.Column="1" Style="{StaticResource SquareInputStyle}" Margin="0,0,5,0" Height="35">
                                            <TextBox x:Name="txtNomActivo" Background="Transparent" BorderThickness="0" VerticalContentAlignment="Center" Padding="5,0" ToolTip="Nombre Activo"/>
                                        </Border>

                                        <Button Grid.Column="2" Content="Guardar" Click="BtnGuardarActivo_Click" Width="80" Height="35" Style="{StaticResource SquareSecondaryButton}"/>
                                    </Grid>

                                    <DataGrid x:Name="gridActivos" Grid.Row="2" AutoGenerateColumns="True"/>
                                </Grid>
                            </Border>
                        </Grid>
                    </TabItem>

                    <!-- PESTAÑA 3: REPORTES -->
                    <TabItem Header="REPORTES">
                        <Grid Margin="0,20,0,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>

                            <StackPanel Orientation="Horizontal" Grid.Row="0" Margin="0,0,0,20">
                                <Button Content="Gastos por Activo" Click="BtnRepGastos_Click" Width="150" Margin="0,0,10,0" Style="{StaticResource SquareSecondaryButton}"/>
                                <Button Content="Matriz Cruzada" Click="BtnRepMatriz_Click" Width="150" Style="{StaticResource SquareSecondaryButton}"/>
                            </StackPanel>

                            <Border Grid.Row="1" Background="#FFFFFF" CornerRadius="8" Padding="10" BorderBrush="#20000000" BorderThickness="1">
                                <DataGrid x:Name="gridReportes" IsReadOnly="True" AutoGenerateColumns="True"/>
                            </Border>
                        </Grid>
                    </TabItem>

                </TabControl>
            </Grid>
        </Border>
    </Border>
</Window>